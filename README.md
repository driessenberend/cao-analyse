# cao-analyse
A semantic search and RAG application to analyse all CAO's in the Netherlands from 2025.

## Database setup
Supabase schema, storage bucket, and RPC are defined in `supabase/migrations/0001_init.sql`.
